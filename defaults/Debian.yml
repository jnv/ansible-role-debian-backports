---
backports_uri: "{% if ansible_distribution_version >= '10' %}https{% else %}http{% endif %}://deb.debian.org/debian"
backports_components: "{{backports_distribution}}-backports main contrib non-free"
